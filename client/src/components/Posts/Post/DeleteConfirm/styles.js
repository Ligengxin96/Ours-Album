import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  title: {
    display: 'flex',
    textAlign: 'center',
    alignItems: 'center'
  },
  iconButton: {
    marginLeft: '3rem'
  }
});
